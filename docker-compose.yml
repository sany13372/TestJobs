services:
  backend:
    build: ./backend
    environment:
      - PORT=3000
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "5173:80"
